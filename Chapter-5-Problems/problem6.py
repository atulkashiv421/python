#create an empty dict,allow 4 friends to enter their favourite language as value and use key as their name .assume the name as unique
s ={}
name = input("Enter student name: ")
lang = input("Enter the language:")

s.update({name: lang})

name = input("Enter student name: ")
lang = input("Enter the language:")

s.update({name: lang})

name = input("Enter student name: ")
lang = input("Enter the language:")

s.update({name: lang})

name = input("Enter student name: ")
lang = input("Enter the language:")

s.update({name: lang})

name = input("Enter student name: ")
lang = input("Enter the language:")

s.update({name: lang})

print(s)
